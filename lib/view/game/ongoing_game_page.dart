import 'package:flutter/material.dart';

class OngoingGameWidget extends StatelessWidget {
  const OngoingGameWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
